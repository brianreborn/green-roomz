# security-monitor-agent

Async mailbox only. Producers push and return immediately; this agent drains in the background.
Do not block specialists. Do not add user-path latency.
Do not replace safety-policy-agent (qwen3guard GGUF on 8188). This is not a GGUF and has no physical backend.
Future sites (CUDA mapped ring, PQFreeBSD MAC) use the same envelope: seq, kind, source, ticket, ts, payload.
